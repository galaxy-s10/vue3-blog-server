import authController from '@/controller/auth.controller';
import userModel from '@/model/user.model';

const getUserStatus = async (ctx) => {
  // authController.getMyAuth();
};

export default getUserStatus;
